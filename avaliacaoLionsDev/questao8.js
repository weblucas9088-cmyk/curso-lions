const prompt = require ('prompt-sync') ();

let palavra = prompt (" Lucas ")
let caracteres = 0

caracteres = split.palavra

console.log (`A quantidade de caracteres é ${caracteres}`)

//Esqueci o codigo de aparecem quantas vezes a parece as caracteres 