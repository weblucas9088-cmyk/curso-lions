const prompt = require ('prompt-sync') ();

let palavra = prompt (" Ola mundo ")

palavra.forEach(palavra => {
    if (palavra = palavra + split ) {
        console.log (" A palavra é lida de trás para frente ")
    }else {

    }
});

//Aqui tinha que usar outro codigo para o sistema ver se é possiver ler de trás para frente mais esqueci *_*