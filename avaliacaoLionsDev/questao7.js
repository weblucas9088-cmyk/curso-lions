const prompt = require ('prompt-sync') ();

let lista = [100, 200, 300, 400, 500, 600, 100];

for (let i = 0; i < lista.length; i++) {
    if (lista.push = lista && 100) 
    [i].forEach(lista => {
        console.log (`O número repetido foi o ${lista}`)
    });
}
console.table (lista)

//ficou feio mais funciona