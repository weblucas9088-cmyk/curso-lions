const prompt = require ('prompt-sync') ();

let numeros = [393, 233, 131, 932, 199, 234]
let analisar = 0

for (let i = 0; i < numeros.length; i++) {
      if (numeros.push = analisar) {
        console.log (`  O maior número é ${numeros.pop}`)
      }else {
        console.log (`O menor é ${numeros}`)
      }
}
console.log (numeros)

//Não consegui mostrar qual é maior ou menor